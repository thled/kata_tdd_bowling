{
    "require": {
    },
    "require-dev": {
        "phpunit/phpunit": "^9.2"
    },
    "autoload": {
        "psr-4": {
            "App\\": "src/",
            "App\\Tests\\": "tests/"
        }
    },
    "scripts": {
        "bootstrap": [
            "cp .php_cs.dist .php_cs",
            "@composer install"
        ],
        "ci": [
            "@lint",
            "@phpstan",
            "@test"
        ],
        "lint": "php-cs-fixer fix --config=.php_cs -vvv --dry-run --allow-risky=yes",
        "fix": "php-cs-fixer fix --config=.php_cs --allow-risky=yes",
        "phpstan": "phpstan analyse --configuration phpstan.neon",
        "test": "phpunit tests --testdox --colors=always",
        "test:watch": [
            "Composer\\Config::disableProcessTimeout",
            "phpunit-watcher watch < /dev/tty"
        ],
        "infection": "infection --test-framework-options=\"tests\" --initial-tests-php-options=\"-d zend_extension=xdebug.so\""
    }
}
